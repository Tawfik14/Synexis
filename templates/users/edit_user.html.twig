{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur{% endblock %}

{% block body %}
  <h1>✏️ Modifier l'utilisateur {{ user.pseudo }}</h1>

  {{ form_start(form) }}
    <div style="max-width: 600px;">
      {{ form_row(form.firstname) }}
      {{ form_row(form.lastname) }}
      {{ form_row(form.pseudo) }}
      {{ form_row(form.email) }}
      {{ form_row(form.role) }}
      {{ form_row(form.points) }}
      {{ form_row(form.level) }}
    </div>
    <button type="submit" class="btn btn-success mt-3">💾 Sauvegarder</button>
    <a href="#" onclick="history.back()" class="btn btn-secondary mt-3">↩️ Retour</a>
  {{ form_end(form) }}
{% endblock %}
